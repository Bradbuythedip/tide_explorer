{
  "name": "prevblock",
  "private": true,
  "version": "0.0.0",
  "description": "Post-quantum block explorer for Tidecoin. prevblock.com",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "npx --yes pnpm@9.12.0 install --no-frozen-lockfile && npx --yes pnpm@9.12.0 --filter @prevblock/shared build && npx --yes pnpm@9.12.0 -C packages/frontend build",
    "build:libs": "pnpm -C packages/shared build && pnpm -C packages/rpc-client build",
    "build:all": "pnpm -r --filter './packages/*' build",
    "dev": "pnpm -r --parallel --filter './packages/*' dev",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "test": "pnpm -r --filter './packages/*' test",
    "lint": "pnpm -r --filter './packages/*' lint",
    "clean": "pnpm -r --filter './packages/*' clean && rm -rf node_modules"
  },
  "devDependencies": {
    "next": "14.2.35",
    "typescript": "^5.6.3"
  }
}

